
{{ form_start(form, {'attr': {'id': 'contentForm', 'novalidate': 'novalidate'}}) }}
    {{ form_row(form.content) }}
<div class="row text-center" id="commentForm">
    <div class="col-sm-6">
        <a href="{{ path('products_index', {'user': app.user.id, 'id': product.id}) }}" class="btn btn-outline-primary">Retour</a>
    </div>
    <div class="col-sm-6">
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>
</div>
{{ form_end(form) }}
